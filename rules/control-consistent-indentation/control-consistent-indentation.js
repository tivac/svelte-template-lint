"use strict";

// TODO: enforce that the closing tag of a control block like {#each} {/each}
// is indented the same amount as the starting tag

module.exports = (id, ast, meta) => {
    console.log(ast);
};
